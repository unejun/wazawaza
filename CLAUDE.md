# wazawaza LP — Claude Code 作業ガイド

このリポジトリで作業する際の前提と、これまでの経緯・判断・残タスクをまとめる。
**新しいセッションは、人間でもAIでも、まずこのファイルを読むこと。**

---

## このプロジェクトの位置づけ — #001 IKUSA は「型」である

- wazawaza の第1弾商品 **#001 IKUSA** のLP。本番は **wazawaza.tokyo**。
- 【重要】このLPは「型」である。今後 #002 / #003 と続く商品LPは、
  表面のデザイン（色・書体・レイアウト・画像）だけを変え、
  裏側の構造・仕組みは基本的にこれを踏襲する。
- 以下は「再利用する資産」として設計・維持されている：
  - **リーガルページの構成**（特商法 / Privacy Policy / Terms of Use / Cookie Policy の4点セット + 二重配置の仕組み）
  - **アクセシビリティ対応の実装**（フォーカストラップ、KV停止ボタン、コントラスト設計など）
  - **フォーム連携（Typeform）の構造**
  - **フォルダベースのクリーンURL構成**
- 次にLPを作る人（人間でもAIでも）が、ここを読めば
  「なぜこうなっているか」を理解できる状態にすること。

### 新しい商品LPを作るときの指針
- リーガルページは、この4つを土台に商品名・日付だけ差し替える。
- アクセシビリティは、最初から設計に織り込む（後から直すと、デザインとの
  衝突で手戻りが大きい。#001 では文字サイズをめぐって MN と協議が必要になった）。
- 黄色背景を使うなら、コントラストの制約を先にデザイナーへ共有する。
- KV動画を使うなら、停止ボタンの設置を前提にレイアウトする。

---

## ファイル構成の注意

### リーガルの構成 — 独立ページは privacy-policy だけ、他はモーダルのみ

リーガル文書は4つ（特商法 / Privacy Policy / Terms of Use / Cookie Policy）。
表示経路は次のとおり（2026-07 に再編）。

- **全4つ**：各作品の `index.html` 内に埋め込まれたテンプレート
  （`<script type="text/html" id="embed-legal-*">`）で、フッターの `data-legal`
  リンクから **LP内モーダル表示**する。
- **Privacy Policy だけ**：独立ページ `/privacy-policy/index.html`
  （wazawaza.tokyo/privacy-policy）も持つ。**理由：Typeform の同意フローから
  外部リンクで飛ばす必要があるため**（モーダルには外部から飛ばせない）。
  全作品共通なので 002 以降も同じ URL で済む。
- **Cookie / Terms / 特商法**：独立ページは廃止（削除済み）。**LP内モーダルのみで表示**。
  フッターのリンクは `href="#"` ＋ `data-legal` でモーダルを起動する
  （`#` は no-JS 時のフォールバック。遷移先ページは無い）。

修正時の注意：
- **Privacy Policy を直すときは2箇所**（独立ページ `/privacy-policy/` と
  埋め込み `embed-legal-privacy`）を一致させる。
- **Cookie / Terms / 特商法 は埋め込みテンプレート1箇所だけ**（独立ページなし）。
- 特商法は作品ごとに内容（価格・エディション数）が異なる。
  Privacy / Cookie / Terms は全作品共通。

### 廃止済みドメイン
`wazawaza-japan.com` は完全に廃止。本番は **wazawaza.tokyo** のみ。

### 環境（ローカル検証）
- ローカルサーバー：`node <scratchpad>/audit/server.js ~/Documents/GitHub/wazawaza` → `http://localhost:8848/`
- axe / Lighthouse は scratchpad の Node 環境（ローカル展開）で実行。
- ffmpeg は無いので、動画フレームは Chrome canvas で抽出。

---

## アクセシビリティ（絶対に壊さない）

2026-07-12 に **WCAG 2.1 AA** 対応を完了。**axe 自動検出違反 0件、Lighthouse 96点。**

- **ピンク文字**（SHIPPING SCHEDULE / APPLICATION WINDOW / Chapter 01-03 /
  "a dream…" / インタビュー話者名）は **「19px かつ 太字」が必須**。
  細字にすると基準が 3:1 → 4.5:1 に戻り違反になる。
- モーダル / ライトボックス / Typeform には**フォーカストラップ実装済み。触るな。**
- KV動画には**一時停止ボタン**と `prefers-reduced-motion` 対応がある。
- **画像を移動・削除するときは `alt` も一緒に移動・削除する。**
- 変更後は**必ず axe を実行**し、違反0件のままか確認（インタビュー展開状態でも）。
- **【最重要の教訓】axe が検出できない違反が過去に5件あった**
  （すべて手動キーボード操作で発見）。ツールの結果だけで「問題なし」と
  判断せず、実装後は**必ず実機で触って確認すること。**

---

## 判断の記録（再議論しない）

- **Typeform のタブ順**：iframe が別ドメインで完璧に制御できない。
  背後を `inert` 化した現状で WCAG は満たす。→ **これ以上は追わない。**
- **ギャラリーの矢印キースクロール**：不要。ページャー P1〜P12 で全画像に到達でき、
  **WCAG 2.1.1 を満たしている。**
- **`alt` の "plate"**：美術書の「図版」の意。正しい英語なので**変更不要。**
- **タイトル画像内のカタカナ**：純粋な装飾。コントラスト基準の**免除対象。**
- **KV動画の文字**：動画に焼き込まれた映像。**CSS では変更できない。**
  （文字が灰色に見えた原因は hero の暗幕 `.c-hero::after` で、撤去済み。）
- **ピンクの色番号2種類**（画像 `#ed1d79` / テキスト `#e81878`）：
  視覚的にほぼ同じため**放置。**
- **セクションタイトルの画像テキスト化（WCAG 1.4.5）**：見送り。
  1枚に時間がかかり費用対効果が悪い。代替は **PNG→SVG 差し替え**
  （拡大してもぼやけず、見た目は完全維持。MN に素材を依頼するだけ）。
  分析結果を残すため `text-headings` ブランチは削除せず保持。

---

## コマンド実行の運用ルール

`.claude/settings.json` に設定済み。ここは方針の記録。

### 自動実行してよい（確認不要・読み取り専用）
`grep` / `find` / `cat` / `awk` / `ls` / `head` / `tail` / `wc` /
`git log` / `git diff` / `git status` / `git branch` / `git show` / `git ls-tree` /
`curl`（ローカル確認） / `node`（検証）

### 必ず確認を取る（変更・破壊・不可逆）
`git push` / `git commit` / `git merge` / `git checkout` / `git reset` / `git rm` /
`rm` / `mv` / `sed` / ファイル書き込み（`Write` / `Edit`）

### コマンドの種類では中身の妥当性は測れない
過去に「コマンドは安全でも中身が誤り」の事故があった：
修正先がゴミファイルだった／古いドメインが残っていた／
インタビュー写真の差し替えが指示と逆だった。
だから**実装後は必ずユーザーが実機確認し、push／マージ前は立ち止まる。**

---

## 作業の進め方

- **`main` = 本番。直接コミットしない。必ずブランチを切る。**
- **デザインに影響する変更は別ブランチに分ける**
  （例：`a11y-base` = デザイン中立、`kv-tuning` / `contrast-*` = デザイン変更）。
- **push と `main` へのマージはユーザーが GitHub Desktop / Web で行う。**
  Claude Code からの push は認証が通らない。
- スクショ・測定の詳細報告は不要。実装してコミットまで進め、
  ユーザーが実機で確認して判断する。

---

## 現在の状態（最終更新：2026-07-15）

### 本番反映済み
- アクセシビリティ対応（178件 → 0件）、英文校正22件、
  SHIPPING SCHEDULE 日付（Oct → Aug）、Instagram 公式リンク、
  古いドメイン除去、リーガルページ間のリンク切れ修正。
- 2026-07-15 の修正3件（インタビュー写真の再修正、vision 段落追加、
  limited-edition ハイフン5件）も **`fixes-0714`** で対応済み。

### 残タスク（急ぎでない）
- **アクセシビリティ声明ページの作成**（違反ではないが、法的立場を強化）
  - 米国：法律上の要件ではないが、問い合わせ窓口があると訴状の前に連絡が
    来る可能性が生まれ、訴訟の予防になる。
  - EU（EAA）：声明ページは法律上の義務。現在は零細企業免除の見込みだが、
    事業が成長して免除枠を超えたら必須になる。
  - 英語原稿は作成済み（別ファイル）。
  - **【着手の前提】専用メールアドレスの作成が必要**（例 `accessibility@wazawaza.tokyo`。
    既存の問い合わせ受信箱への転送で可）。
  - **【重要】窓口をフォームにしてはいけない。** フォーム自体が使えない人が
    問題を報告するための窓口だから。また Typeform を外部から制御できないと
    声明文中で明記する以上、それを唯一の窓口にするのは自己矛盾になる。→ **窓口はメール、フォーム不可。**
- LEGAL 独立ページ4つの単独 axe 診断（7/12 は index.html のみ実施）。
- リーガルの二重管理の解消（原本を1つにするのが理想）。
- セクションタイトルの SVG 化（上記「判断の記録」参照）。
- 未使用 `legal-*.html` の整理。
