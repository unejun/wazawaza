# CLAUDE.md — wazawaza LP 作業ガイド

このリポジトリで作業する際の前提と、これまでの経緯・判断・残タスクをまとめる。
新しいセッションはまずこのファイルを読むこと。

---

## 作業の前提（必ず守る）

- **`main` = 本番。直接コミットしない。**
- **作業は必ずブランチを切って行う。**
- **デザインに影響する変更は、別ブランチに分ける**（例：`a11y-base` = デザイン中立、`kv-tuning`/`contrast-*` = デザイン変更）。
- **変更後は必ず axe で退行チェックする**（インタビュー展開状態でも確認。自動検出違反 0件を維持）。
- **実装後は必ずジュン（ユーザー）が実機（`localhost:8848`）で確認する。** 特にキーボード操作・見た目は本人確認を待つ。
- push は認証の都合でジュンが GitHub Desktop で行う。main へのマージも GitHub の画面で行う。

### 補足（環境）
- ローカルサーバー：`node <scratchpad>/audit/server.js ~/Documents/GitHub/wazawaza` → `http://localhost:8848/`
- axe / Lighthouse は scratchpad の Node 環境（ローカル展開）で実行。ffmpeg は無いので動画フレームは Chrome canvas で抽出。

---

## リーガルページは「2箇所」にある — 修正時は必ず両方を書き換える

リーガルページ4つ（特商法 / Privacy Policy / Terms of Use / Cookie Policy）は、同じ内容が2箇所に存在する。片方だけ直すと内容がズレる。

- **【1】`index.html` 内に埋め込まれたもの**（LP上でモーダル表示）
- **【2】各フォルダ内の `index.html`**（独立ページ）
  - `/privacy-policy/index.html` → wazawaza.tokyo/privacy-policy
  - `/cookie-policy/index.html`  → wazawaza.tokyo/cookie-policy
  - `/terms/index.html`          → wazawaza.tokyo/terms
  - `/tokushoho/index.html`      → wazawaza.tokyo/tokushoho

独立ページが必要な理由：Typeform のフロー内からリンクで飛ばすため。モーダルには外部から飛ばせない。

修正後は、**【1】と【2】の本文が一致しているか必ず確認すること。**

### 未使用ファイル（触るな）

以下は本番で公開されていない古いファイル。編集しても本番に反映されない。

  `legal-terms.html` / `legal-privacy.html` / `legal-cookies.html` / `legal-tokushoho.html`

---

## ✅ 完了・本番反映済み（2026-07-12）

- **WCAG 2.1 A/AA 自動検出違反 178件 → 0件**
- landmark / 見出し構造の整備、`<h1>` 追加
- フォーカス管理（法務モーダル・Typeform ポップアップのフォーカストラップ）
- ギャラリー拡大（ライトボックス）のキーボード対応＋フォーカストラップ
- KV動画の一時停止ボタン（`prefers-reduced-motion` 対応込み）
- コントラスト調整（ページャー番号／ピンク文字の large text 化）
- （デザイン）KV停止ボタン配色変更・hero 暗幕撤去（MN依頼）

---

## 📌 重要な判断の記録（あとで蒸し返さないため）

- **Typeform のタブ順**：iframe が別ドメインのため完璧には制御できない。背後を `inert` 化してブラウザUIへの脱出を防いだ現状で WCAG は満たす。→ **これ以上は追わない**
- **ギャラリーの矢印キースクロール**：追わない。ページャー P1〜P12 で全画像に到達でき、**WCAG 2.1.1 は満たしている**
- **ピンク文字**：色を変えず 19px 化で解決。**「19px かつ 太字」がセット**。細字にすると基準が 3:1 → 4.5:1 に戻り破綻する
- **`alt` の "plate"**：美術書の「図版」の意。正しい英語なので**変更不要**
- **タイトル画像内のカタカナ**：純粋な装飾。コントラスト基準の**免除対象**
- **KV動画の作り直し**：不要。文字が灰色に見えた原因は hero の暗幕（`.c-hero::after`）で、**撤去済み**
- **ピンクの色番号2種類**（画像 `#ed1d79` / テキスト `#e81878`）：視覚的にほぼ同じため**放置**

---

## 🚧 残タスク

- [ ] **【見送り】セクションタイトルのテキスト化**（WCAG 1.4.5）
  - GALLERY 1枚に1時間かかり、費用対効果が悪いため見送り
  - 自動検出されず、`alt` も正しく付いているため実害は限定的
  - 困るのは拡大表示する弱視ユーザー（画像なのでぼやける）
  - **【代替案】PNG を SVG に差し替える** → 拡大してもぼやけない。見た目は完全に維持。MN に素材を依頼するだけで済む
  - `text-headings` ブランチは削除せず保持（GALLERY の分析結果があるため）
- [ ] **アクセシビリティ声明ページの作成**（違反ではないが、法的立場を明確に強化）
  - 米国：法律上の要件ではないが、問い合わせ窓口があると訴状の前に連絡が来る可能性が生まれる。訴訟の予防になる
  - EU（EAA）：声明ページは法律上の義務。現在は零細企業免除の見込みだが、事業が成長して免除枠を超えたら必須になる
  - 英語原稿は作成済み（別ファイル）
  - **【着手の前提】専用メールアドレスの作成が必要**
    - 例：`accessibility@wazawaza.tokyo`
    - 既存の問い合わせ受信箱への転送で構わない（運用は増えない）
    - **【重要】窓口をフォームにしてはいけない。** フォーム自体が使えない人が、アクセシビリティの問題を報告するため。また Typeform は外部ツールで制御できないと声明文中で明記する以上、それを唯一の窓口にするのは自己矛盾になる

---

## 位置づけ — #001 IKUSA は「型」である

このLPは、wazawaza の第1弾商品 #001 IKUSA のページだが、今後 #002、#003 と続く商品LPの「型」として設計されている。

デザイン（色・書体・レイアウト）は商品ごとに変わる。しかし、以下は再利用する資産である。

### 再利用する資産

**1. リーガルページの構成**
- 特商法 / Privacy Policy / Terms of Use / Cookie Policy の4点セット
- LP内モーダル + 独立ページの二重構成（Typeform のフローから外部リンクで飛ばす必要があるため）
- 英文の法務文言

**2. アクセシビリティ対応の設計**
- 米国 ADA 第3編（WCAG 2.1 AA）への対応が必要 → 英語サイトで米国顧客に販売する以上、企業規模による免除はない
- モーダル / ライトボックス / Typeform のフォーカストラップ実装
- KV動画の一時停止ボタン + prefers-reduced-motion 対応
- コントラスト設計（黄色背景 × ピンク文字は 19px + 太字が必須）
- ランドマーク / 見出し構造 / aria-label の設計

**3. 検証のプロセス**
- axe-core + Lighthouse による自動診断
- **【重要】自動ツールだけでは不十分。** 過去に、ツールが検出できないレベルA違反が5件、手動のキーボード操作で見つかっている。必ず実機で触って確認すること。

### 新しい商品LPを作るときの指針

- リーガルページは、この4つを土台に商品名・日付だけ差し替える
- アクセシビリティは、最初から設計に織り込む（後から直すと、デザインとの衝突で手戻りが大きい。#001 では文字サイズをめぐって MN と協議が必要になった）
- 黄色背景を使うなら、コントラストの制約を先にデザイナーへ共有する
- KV動画を使うなら、停止ボタンの設置を前提にレイアウトする
